import { typedef } from "./typedef";
import { quires } from "./quires";
import { mutations } from "./mutations";
import { resolvers } from "./resolver";

export const User = { typedef, quires, mutations, resolvers };
